# 🔴 ИСПРАВЛЕНИЕ: no such table: auth_user (500 Error)

## Проблема
Ошибка: `django.db.utils.OperationalError: no such table: auth_user`

Это означает, что **миграции Django не были применены** на сервере Render.

## ✅ РЕШЕНИЕ

### Шаг 1: Обновить файлы (УЖЕ СДЕЛАНО)
Файлы `render.yaml` и `Procfile` обновлены для автоматического выполнения миграций.

### Шаг 2: Перезапустить деплой

**Вариант А: Через Git (рекомендуется)**
```bash
cd C:\Users\User\Documents\Django\TODO
git add render.yaml Procfile
git commit -m "Fix: Add migrations to start command"
git push
```
Render автоматически перезапустит деплой.

**Вариант Б: Manual Deploy в Render Dashboard**
1. Зайдите в Render Dashboard → ваш сервис
2. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**
3. Дождитесь завершения

### Шаг 3: Проверить логи
После деплоя проверьте логи в Render Dashboard:
- Должна появиться строка: `Operations to perform: Apply all migrations: ...`
- Не должно быть ошибок о таблицах

### Шаг 4: Создать суперпользователя (если нужно)
После успешного деплоя, в Render Shell выполните:
```bash
python manage.py createsuperuser
```

## Что было изменено:

1. **render.yaml**: 
   - Миграции перемещены из `buildCommand` в `startCommand`
   - Добавлен `ALLOWED_HOSTS`
   
2. **Procfile**:
   - Упрощен, миграции выполняются перед запуском gunicorn

## Почему это работает:

- Build Command выполняется во время сборки, когда база данных может быть недоступна
- Start Command выполняется при запуске сервиса, когда база данных уже доступна
- Миграции теперь выполняются при каждом старте (безопасно, Django проверяет, применены ли они)

## После исправления:

Сайт должен работать! Вы сможете:
- Зарегистрироваться
- Войти в систему
- Создавать задачи
- Использовать все функции

---

**Если проблема остается:**
1. Проверьте логи в Render Dashboard
2. Убедитесь, что `ALLOWED_HOSTS` включает ваш домен
3. Проверьте, что переменные окружения установлены правильно

