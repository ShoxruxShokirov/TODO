# 🔧 Исправление ошибки 400 (Bad Request)

## Проблема:
Ошибка 400 Bad Request обычно означает, что домен не указан в `ALLOWED_HOSTS`.

## Решение:

### В Render Dashboard → Environment Variables:

Добавьте или обновите переменную:

- **KEY:** `ALLOWED_HOSTS`
- **VALUE:** `todo-nsz6.onrender.com`

**Или** если у вас другой домен, замените на ваш:
- `your-app-name.onrender.com`

---

## Если нужно разрешить несколько доменов:

Можно указать через запятую:
```
todo-nsz6.onrender.com,localhost,127.0.0.1
```

---

## После добавления переменной:

1. Сохраните изменения
2. Дождитесь автоматического перезапуска (или сделайте Manual Deploy)
3. Обновите страницу

---

## Альтернативное решение (если не помогает):

Если проблема сохраняется, проверьте:

1. **DEBUG** должен быть `False` в production
2. **ALLOWED_HOSTS** должен содержать ваш домен Render
3. Перезапустите сервис в Render

После этого ошибка 400 должна исчезнуть!

