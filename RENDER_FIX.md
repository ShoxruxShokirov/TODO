# 🔧 Исправление настроек Render

## Где найти настройки в Render Dashboard:

1. **Зайдите в ваш сервис** (Web Service)
2. **Перейдите во вкладку "Settings"** (Настройки)
3. **Прокрутите вниз до раздела "Build & Deploy"**

## Что нужно проверить и исправить:

### 1. Environment (Окружение)
- Если не видите "Environment", возможно оно называется "Runtime"
- Должно быть: **Python 3** или **Python**
- НЕ должно быть: Go, Node, Ruby и т.д.

### 2. Build Command (Команда сборки)
Замените на:
```
pip install --upgrade pip && pip install -r requirements.txt && python manage.py collectstatic --noinput
```

### 3. Start Command (Команда запуска)
Замените на:
```
gunicorn todo_project.wsgi:application
```

### 4. Root Directory
- Должно быть **ПУСТО**

### 5. Branch
- Должно быть: **main**

---

## Альтернативный способ (если не нашли Environment):

Если вы не можете найти поле "Environment", возможно Render определяет его автоматически. В этом случае:

1. **Удалите старый сервис** (или создайте новый)
2. **При создании нового сервиса:**
   - После подключения репозитория
   - Render спросит "What type of service is this?"
   - Выберите **"Web Service"**
   - Затем выберите **"Python 3"** в списке Runtime/Environment
3. **Затем укажите правильные команды**

---

## Если уже создан сервис:

1. Зайдите в **Settings** вашего сервиса
2. Найдите раздел **"Build & Deploy"**
3. Обновите **Build Command** и **Start Command** (см. выше)
4. **Сохраните изменения** (Save Changes)
5. Нажмите **"Manual Deploy"** → **"Deploy latest commit"**

---

## Быстрая проверка:

Откройте логи деплоя и проверьте:
- ✅ Должно быть: "Using Python version..."
- ❌ НЕ должно быть: "Using Go version..."

Если видите "Using Go version", значит Environment все еще установлен как Go, и нужно его изменить.

